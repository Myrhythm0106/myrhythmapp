import React from 'react';
import { CaptureDeliverableView } from '@/components/memoryBridge/capture-brief/CaptureDeliverableView';

export default function LaunchCaptureResult() {
  return <CaptureDeliverableView />;
}
