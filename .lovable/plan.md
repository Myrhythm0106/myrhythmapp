## Hero title swap

Replace the two-line headline on `/prototype` with the chosen empowering title.

**File:** `src/pages/prototype/PrototypeLanding.tsx`

- Replace the current H1 ("Your personal assistant after a brain injury.") with:
  - **H1:** "Think clearly. Live fully."
  - **Sub-line (small, slate-500, above H1 as an eyebrow):** "For anyone carrying a heavy cognitive load."
- Keep the existing supporting paragraph unchanged ("Record a conversation…").
- No other screens, styles, or routes touched. Quiet Power styling preserved (slate-900, no gradient, no Sparkles).

**Acceptance:** `/prototype` shows the new title above the existing description, condition-neutral but empowering, with the existing 4-card grid and CTA unchanged.