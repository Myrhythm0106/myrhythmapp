# Keep the thread line as is — no changes

## Decision

The thread line on `/launch/landingpage` stays exactly as it is:

> One thread. From source, through follow-through, to completion.

"to completion" was chosen over "until completion" — it reads as decisive and final. The existing line already expresses this, so no copy or code changes are needed.

## Scope

- No file edits.
- The story flow (six steps, ending "Follow through—and mark it complete") remains untouched.
